# dotfiles

Config `~/.config/chezmoi/chezmoi.toml`

```toml
[data]
  type = "work"

  git.personal.email =
  git.personal.signingkey =

  git.work.dir =
  git.work.email =
  git.work.signingkey =
````

